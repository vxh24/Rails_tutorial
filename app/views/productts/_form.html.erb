<%= form_with(model: productt) do |form| %>
  <% if productt.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(productt.errors.count, "error") %> prohibited this productt from being saved:</h2>

      <ul>
        <% productt.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
